package parser.common

enum class KtType {
    FILE,
    CLASS,
    FUNCTION,
    BODY,
    MODIFIER,
    TYPE,
    VARIABLE,
    VARTYPE,
    ID,
    EXPRESSION,
    DECL,
    IF,
    ELSE,
    CONTROLFLOW,
    IMPORT,
    STATEMENT,
    FOR
}
